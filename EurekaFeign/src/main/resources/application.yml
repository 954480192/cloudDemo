eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
server:
  port: 8765
spring:
  application:
    name: service-feign

feign:
  hystrix:
    enabled: true    # 开启Hystrix熔断功能  需要回调类（DemoFallback）作为容错方案
